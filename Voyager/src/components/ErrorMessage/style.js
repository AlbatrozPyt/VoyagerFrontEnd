import { Text } from "react-native";
import styled from "styled-components";

export const TextError = styled(Text)`
    position: "absolute";
    bottom: -17;
    left: 10;
    color: red;
    align-self: center;
    font-size: 16;
`